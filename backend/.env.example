# Copy this file to .env (or export these as real environment variables)
# and fill in the values from console.cognodb.com after creating your instance.
# NEVER commit the real .env file.

COGNODB_URI=bolt+s://db-f8989861.databases.cognodb.com:7687
COGNODB_USERNAME=cognodb
COGNODB_PASSWORD=23a5a2b876560a901c5f1f0e589
COGNODB_DATABASE=neo4j

# Origin allowed to call the API (your frontend dev server or deployed URL)
ALLOWED_ORIGIN=http://localhost:5173

PORT=8080
